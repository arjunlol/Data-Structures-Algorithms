//sort stack so smallest on the
const Stack = require('./3.stack.js')

//push
//pop
//isempty
//peak

const sort = (stack) => {
  //additional temp stack
  let tempStack = new Stack();
  let current = stack.head
  while(!current) {

  }
}