//assume have isSubstring
//s1 & s2, check if s2 is rotation os s1 using
//only 1 isSubstring
//waterbottle rotation is of erbottlewat
isRotation = (str1, str2) => {
  let index2 = 0;
  for (let i = 0; i < str.length; i++) {
    if (str1[i] === str2[index2]) {

    }
  }
}

